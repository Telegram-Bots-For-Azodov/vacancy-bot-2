services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: vacancy-bot:latest
    container_name: vacancy-bot
    restart: always
    env_file: .env
    environment:
      - DB_PATH=/app/data/vacancy.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs