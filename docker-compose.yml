services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: vacancy-bot:latest
    container_name: vacancy-bot
    restart: always
    network_mode: host
    env_file: .env
    environment:
      # Baza Docker ichida emas — host'ning loyiha root papkasida (./vacancy.db)
      - DB_PATH=/data/vacancy.db
    volumes:
      - .:/data
      - ./logs:/app/logs